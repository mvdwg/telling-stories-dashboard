/*jshint node:true*/
'use strict';

module.exports = function(environment /*, appConfig */) {
  var ENV = {
    modulePrefix: 'telling-stories-engine',
    environment: environment
  };

  return ENV;
};
